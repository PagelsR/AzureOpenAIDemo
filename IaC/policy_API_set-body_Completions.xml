<policies>
    <inbound>
        <base />
        <!--<choose>
            <when condition="@(context.Request.Body.As<JObject>() == null || context.Request.Body.As<JObject>().Count == 0)">
                --><!-- Request body is empty, add a new one --><!--
                <set-body>@{
					JObject requestBody = new JObject();
					requestBody["prompt"] = "What is the meaning of life?";
					requestBody["model"] = "text-davinci-003";
					requestBody["max_tokens"] = 100;
					requestBody["temperature"] = 0;
					return requestBody.ToString();
					}</set-body>
            </when>
        </choose>-->
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>